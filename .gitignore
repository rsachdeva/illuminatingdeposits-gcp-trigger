# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# from goenv
.go-version

checkzip/

# terraform files
**/deploy/terraform/.terraform.lock.hcl
**/deploy/terraform/.terraform/
**/deploy/terraform/terraform.tfstate
**/deploy/terraform/terraform.tfstate.backup
**/deploy/terraform/.terraform.tfstate.lock.info
# inside terraform files for terraform keys for service account
**/deploy/terraform/keys*.json
# inside terraform files zip file uploaded to cloud storage already
**/deploy/terraform/*.zip

# excalidraw
*.excalidraw


# vendor
**/vendor

# samples to trigger
gcf_upload/httprequestfortrigger/sample-myinvest-*
gcf_upload/httprequestfortrigger/env-curl-httpreq-successful-*

sendgrid*
*_email*
